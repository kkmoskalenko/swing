define(function () {
    class Pendulum {
        /**
         * Создаёт маятник
         */
        constructor() {

        }

        /**
         * Рисует маятник в заданном контексте
         * @param context Контекст 2D рендеринга для элемента canvas
         */
        draw(context) {

        }

        /**
         * Определяет, находится ли данная точка внутри фигуры
         * @param mx Координата x точки
         * @param my Координата y точки
         */
        contains(mx, my) {

        }
    }

    return Pendulum;
});